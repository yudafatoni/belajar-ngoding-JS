// JS SYNTAX BASIC

// Variable = Var(Jadul, globalscope), Let(Value dpat diubah), Const(value tdk dapat diubah) 

var nama = "Budi";
let skor = 10;
const pi = 3.14;


// TIPE DATA : Primitive dan Referensi
// Primitif terdiri dari beberapa jenis data yang menyimpan nilai secara langsung (Number, String, Boolean, Symbol)
// Refernsi tipe data yang lebih kompleks dan menyimpan referensi ke lokasi memori dimana data sebenarnya disimpan.(Array, Object, Function)